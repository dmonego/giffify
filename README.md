Interface to create gifs with ffmpeg. `process.js` runs it, `index.js` is the interface to handle cropping and scrubbing. 

`ffmpeg` needs to be installed separately - on OSX, `brew install ffmpeg`

Not ready.